---
// Service Information Component
---

<section class="service-section" id="service-info">
  <div class="service-container">
    <div class="service-grid stagger-children">
      <!-- Sunday Service -->
      <div class="service-card">
        <div class="card-icon">
          <i class="fas fa-clock"></i>
        </div>
        <h3 class="card-title">Sunday Service</h3>
        <p class="card-content">
          12:30 PM - Join us for worship and the Word
        </p>
      </div>

      <!-- Location -->
      <div class="service-card">
        <div class="card-icon">
          <i class="fas fa-map-marker-alt"></i>
        </div>
        <h3 class="card-title">Location</h3>
        <p class="card-content">
          5101 Rolesville Road, Wendell, NC<br>
          (Follow signs for parking)
        </p>
      </div>

      <!-- What to Expect -->
      <div class="service-card">
        <div class="card-icon">
          <i class="fas fa-heart"></i>
        </div>
        <h3 class="card-title">What to Expect</h3>
        <p class="card-content">
          Warm welcome, contemporary worship, and biblical teaching. Casual attire welcome.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .service-section {
    padding: 5rem 1.5rem;
    background-color: white;
  }

  .service-container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .service-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  @media (min-width: 768px) {
    .service-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .service-card {
    background-color: white;
    border-radius: 12px;
    padding: 2.5rem 2rem;
    text-align: center;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    border-top: 3px solid var(--color-teal-mid);
    transition: all 0.3s ease;
  }

  .service-card:hover {
    box-shadow: 0 10px 30px rgba(45, 115, 115, 0.2);
    transform: translateY(-4px);
    border-top-color: var(--color-teal-dark);
  }

  .card-icon {
    width: 4rem;
    height: 4rem;
    background: linear-gradient(135deg, var(--color-gold), var(--color-gold-hover));
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.5rem;
  }

  .card-icon i {
    font-size: 1.75rem;
    color: white;
  }

  .card-title {
    font-family: var(--font-family-secondary);
    font-size: 1.5rem;
    font-weight: 200;
    color: var(--color-primary-dark);
    margin-bottom: 1rem;
  }

  .card-content {
    color: var(--color-text-dark);
    line-height: 1.7;
    font-size: 1rem;
  }
</style>
